'use client';

import React from 'react';
// Header import removed; Header is rendered via page for mobile only
import { PlayerBar } from './PlayerBar';

export function Navbar() {
  return (
    <>
      {/* Header omitted; mobile header rendered in page */}
    </>
  );
}

export { PlayerBar };
