import { createClient } from '@supabase/supabase-js'; import dotenv from 'dotenv'; dotenv.config({ path: '.env.local' }); const supabase = createClient(process.env.NEXT_PUBLIC_SUPABASE_URL, process.env.SUPABASE_SERVICE_ROLE_KEY); async function run() { const query = \CREATE TABLE IF NOT EXISTS verified_releases ( id UUID DEFAULT gen_random_uuid() PRIMARY KEY, youtube_id TEXT UNIQUE NOT NULL, title TEXT NOT NULL, artist TEXT NOT NULL, language TEXT NOT NULL, official_release_date TIMESTAMP WITH TIME ZONE, discovered_at TIMESTAMP WITH TIME ZONE DEFAULT NOW(), song_metadata JSONB NOT NULL );\; const { error } = await supabase.rpc('exec_sql', { sql: query }); console.log(error || 'Table created successfully!'); } run();
