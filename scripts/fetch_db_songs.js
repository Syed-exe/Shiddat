import { createClient } from '@supabase/supabase-js'; import dotenv from 'dotenv'; dotenv.config({ path: '.env.local' }); const supabaseUrl = process.env.NEXT_PUBLIC_SUPABASE_URL; const supabaseKey = process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY; const supabase = createClient(supabaseUrl, supabaseKey); const LANGUAGES = ['Telugu', 'Tamil', 'Kannada', 'Malayalam', 'Hindi', 'English']; async function run() { for (const lang of LANGUAGES) { const { data } = await supabase.from('verified_releases').select('title, artist').eq('language', lang).order('youtube_published_at', { ascending: false }).limit(5); console.log('\n--- ' + lang + ' ---'); if (data && data.length > 0) data.forEach(d => console.log(d.title + ' by ' + d.artist)); else console.log('None found.'); } } run();
